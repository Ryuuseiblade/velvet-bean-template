export { siteConfig, siteContent, type SiteConfig, type SiteContent } from "@/config/site.config";
export { featuresConfig, type FeaturesConfig } from "@/config/features.config";
export { themeConfig } from "@/config/theme.config";
export type {
  ContactInfoBlock,
  GalleryImage,
  MenuItem,
  NavLink,
  SocialLink,
  Testimonial,
} from "@/config/types";
