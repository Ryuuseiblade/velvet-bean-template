/**
 * Single import path for site content used across the app.
 * Buyers should edit `config/site.config.ts` instead of this file.
 */
export { siteConfig, type SiteConfig } from "@/config/site.config";
export { featuresConfig, type FeaturesConfig } from "@/config/features.config";
